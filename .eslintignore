# don't ever lint node_modules
node_modules/

#or documentation
documentation

# don't lint build output (make sure it's set to your correct build folder name)
dist
# don't lint coverage output
coverage
src/coverage
# don't ever lint documentation
documentation
# don't ever lint ci/cd configuration
ci
# don't ever lint logs configuration
logs

.migrationfiles
.scannerwork
.vscode

